package claseMath;

public class Clasemath {

	/*clase Math
	 Math.sqrt(n): raiz cuadrada de un número
	 Math.pow(base,exponente): potencia de un número
	 Math.round(decimal):  redonde de un número
	 Math.PI: constante de clase con el número PI
	 */
}
